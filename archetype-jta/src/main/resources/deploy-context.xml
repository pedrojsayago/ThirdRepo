<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:tx="http://www.springframework.org/schema/tx" xmlns:p="http://www.springframework.org/schema/p" xmlns:c="http://www.springframework.org/schema/c"
    xmlns:util="http://www.springframework.org/schema/util" xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <!-- Specify our deployment configuration -->
    <util:properties id="jta-poc.localContextOverrideProperties">
    </util:properties>

    <context:property-placeholder ignore-resource-not-found="true" local-override="true"
        location="classpath:deployment-environment.properties" properties-ref="jta-poc.localContextOverrideProperties" />

    <!-- Aggregate our full application context. This example includes three patterns, not all of which will be applicable 
        to every implementation; simply remove those that are not needed for a particular implementation. -->

    <!-- Common components -->
    <import resource="tx-context.xml" />
    <import resource="jms-context.xml" />

    <import resource="service-context.xml" />
    <import resource="remote-client-context.xml" />

    <!-- Pattern 1: Inbound-specific components. These are only required if the application is accepting inbound messages. -->
    <import resource="jms-inbound-context.xml" />
    <import resource="integration-inbound-context.xml" />

    <!-- Pattern 2: Database-specific components. These are only required if the application is dealing with a database as 
        part of its processing. -->
    <import resource="dl-context.xml" />

    <!-- Pattern 3: Outbound-specific components. These are only required if the application is transmitting outbound messages. -->
    <import resource="jms-outbound-context.xml" />
    <import resource="integration-outbound-context.xml" />

</beans>
    